# Optional: enable the "Send to model" feature.
# When this is set (server-side only), the Review screen shows a button that
# sends the assembled prompt to the Anthropic Messages API.
# Leave it unset to run the app purely as an assemble-and-export tool.
ANTHROPIC_API_KEY=
